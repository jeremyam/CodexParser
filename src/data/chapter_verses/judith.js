module.exports = {
    1: Array.from({ length: 16 }, (_, i) => i + 1), // Judith 1 has 16 verses
    2: Array.from({ length: 28 }, (_, i) => i + 1), // Judith 2 has 28 verses
    3: Array.from({ length: 10 }, (_, i) => i + 1), // Judith 3 has 10 verses
    4: Array.from({ length: 15 }, (_, i) => i + 1), // Judith 4 has 15 verses
    5: Array.from({ length: 24 }, (_, i) => i + 1), // Judith 5 has 24 verses
    6: Array.from({ length: 21 }, (_, i) => i + 1), // Judith 6 has 21 verses
    7: Array.from({ length: 32 }, (_, i) => i + 1), // Judith 7 has 32 verses
    8: Array.from({ length: 36 }, (_, i) => i + 1), // Judith 8 has 36 verses
    9: Array.from({ length: 14 }, (_, i) => i + 1), // Judith 9 has 14 verses
    10: Array.from({ length: 23 }, (_, i) => i + 1), // Judith 10 has 23 verses
    11: Array.from({ length: 23 }, (_, i) => i + 1), // Judith 11 has 23 verses
    12: Array.from({ length: 20 }, (_, i) => i + 1), // Judith 12 has 20 verses
    13: Array.from({ length: 20 }, (_, i) => i + 1), // Judith 13 has 20 verses
    14: Array.from({ length: 19 }, (_, i) => i + 1), // Judith 14 has 19 verses
    15: Array.from({ length: 14 }, (_, i) => i + 1), // Judith 15 has 14 verses
    16: Array.from({ length: 25 }, (_, i) => i + 1), // Judith 16 has 25 verses
}
