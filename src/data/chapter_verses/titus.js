module.exports = {
    1: Array.from({ length: 16 }, (_, i) => i + 1), // Titus 1 has 16 verses
    2: Array.from({ length: 15 }, (_, i) => i + 1), // Titus 2 has 15 verses
    3: Array.from({ length: 15 }, (_, i) => i + 1), // Titus 3 has 15 verses
}
