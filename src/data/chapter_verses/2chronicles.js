module.exports = {
    1: Array.from({ length: 17 }, (_, i) => i + 1), // 2 Chronicles 1 has 17 verses
    2: Array.from({ length: 18 }, (_, i) => i + 1), // 2 Chronicles 2 has 18 verses
    3: Array.from({ length: 17 }, (_, i) => i + 1), // 2 Chronicles 3 has 17 verses
    4: Array.from({ length: 22 }, (_, i) => i + 1), // 2 Chronicles 4 has 22 verses
    5: Array.from({ length: 14 }, (_, i) => i + 1), // 2 Chronicles 5 has 14 verses
    6: Array.from({ length: 42 }, (_, i) => i + 1), // 2 Chronicles 6 has 42 verses
    7: Array.from({ length: 22 }, (_, i) => i + 1), // 2 Chronicles 7 has 22 verses
    8: Array.from({ length: 18 }, (_, i) => i + 1), // 2 Chronicles 8 has 18 verses
    9: Array.from({ length: 31 }, (_, i) => i + 1), // 2 Chronicles 9 has 31 verses
    10: Array.from({ length: 19 }, (_, i) => i + 1), // 2 Chronicles 10 has 19 verses
    11: Array.from({ length: 23 }, (_, i) => i + 1), // 2 Chronicles 11 has 23 verses
    12: Array.from({ length: 16 }, (_, i) => i + 1), // 2 Chronicles 12 has 16 verses
    13: Array.from({ length: 22 }, (_, i) => i + 1), // 2 Chronicles 13 has 22 verses
    14: Array.from({ length: 15 }, (_, i) => i + 1), // 2 Chronicles 14 has 15 verses
    15: Array.from({ length: 19 }, (_, i) => i + 1), // 2 Chronicles 15 has 19 verses
    16: Array.from({ length: 14 }, (_, i) => i + 1), // 2 Chronicles 16 has 14 verses
    17: Array.from({ length: 19 }, (_, i) => i + 1), // 2 Chronicles 17 has 19 verses
    18: Array.from({ length: 34 }, (_, i) => i + 1), // 2 Chronicles 18 has 34 verses
    19: Array.from({ length: 11 }, (_, i) => i + 1), // 2 Chronicles 19 has 11 verses
    20: Array.from({ length: 37 }, (_, i) => i + 1), // 2 Chronicles 20 has 37 verses
    21: Array.from({ length: 20 }, (_, i) => i + 1), // 2 Chronicles 21 has 20 verses
    22: Array.from({ length: 12 }, (_, i) => i + 1), // 2 Chronicles 22 has 12 verses
    23: Array.from({ length: 21 }, (_, i) => i + 1), // 2 Chronicles 23 has 21 verses
    24: Array.from({ length: 27 }, (_, i) => i + 1), // 2 Chronicles 24 has 27 verses
    25: Array.from({ length: 28 }, (_, i) => i + 1), // 2 Chronicles 25 has 28 verses
    26: Array.from({ length: 23 }, (_, i) => i + 1), // 2 Chronicles 26 has 23 verses
    27: Array.from({ length: 9 }, (_, i) => i + 1), // 2 Chronicles 27 has 9 verses
    28: Array.from({ length: 27 }, (_, i) => i + 1), // 2 Chronicles 28 has 27 verses
    29: Array.from({ length: 36 }, (_, i) => i + 1), // 2 Chronicles 29 has 36 verses
    30: Array.from({ length: 27 }, (_, i) => i + 1), // 2 Chronicles 30 has 27 verses
    31: Array.from({ length: 21 }, (_, i) => i + 1), // 2 Chronicles 31 has 21 verses
    32: Array.from({ length: 33 }, (_, i) => i + 1), // 2 Chronicles 32 has 33 verses
    33: Array.from({ length: 25 }, (_, i) => i + 1), // 2 Chronicles 33 has 25 verses
    34: Array.from({ length: 33 }, (_, i) => i + 1), // 2 Chronicles 34 has 33 verses
    35: Array.from({ length: 27 }, (_, i) => i + 1), // 2 Chronicles 35 has 27 verses
    36: Array.from({ length: 23 }, (_, i) => i + 1), // 2 Chronicles 36 has 23 verses
}
