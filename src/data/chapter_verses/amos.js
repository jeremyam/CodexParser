module.exports = {
    1: Array.from({ length: 15 }, (_, i) => i + 1), // Amos 1 has 15 verses
    2: Array.from({ length: 16 }, (_, i) => i + 1), // Amos 2 has 16 verses
    3: Array.from({ length: 15 }, (_, i) => i + 1), // Amos 3 has 15 verses
    4: Array.from({ length: 13 }, (_, i) => i + 1), // Amos 4 has 13 verses
    5: Array.from({ length: 27 }, (_, i) => i + 1), // Amos 5 has 27 verses
    6: Array.from({ length: 14 }, (_, i) => i + 1), // Amos 6 has 14 verses
    7: Array.from({ length: 17 }, (_, i) => i + 1), // Amos 7 has 17 verses
    8: Array.from({ length: 14 }, (_, i) => i + 1), // Amos 8 has 14 verses
    9: Array.from({ length: 15 }, (_, i) => i + 1), // Amos 9 has 15 verses
}
