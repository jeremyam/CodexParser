module.exports = {
    1: Array.from({ length: 25 }, (_, i) => i + 1), // 1 Peter 1 has 25 verses
    2: Array.from({ length: 25 }, (_, i) => i + 1), // 1 Peter 2 has 25 verses
    3: Array.from({ length: 22 }, (_, i) => i + 1), // 1 Peter 3 has 22 verses
    4: Array.from({ length: 19 }, (_, i) => i + 1), // 1 Peter 4 has 19 verses
    5: Array.from({ length: 14 }, (_, i) => i + 1), // 1 Peter 5 has 14 verses
}
