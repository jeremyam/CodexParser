module.exports = {
    1: Array.from({ length: 18 }, (_, i) => i + 1), // Zephaniah 1 has 18 verses
    2: Array.from({ length: 15 }, (_, i) => i + 1), // Zephaniah 2 has 15 verses
    3: Array.from({ length: 20 }, (_, i) => i + 1), // Zephaniah 3 has 20 verses
}
