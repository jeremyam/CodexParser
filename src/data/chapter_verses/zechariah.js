module.exports = {
    1: Array.from({ length: 21 }, (_, i) => i + 1), // Zechariah 1 has 21 verses
    2: Array.from({ length: 13 }, (_, i) => i + 1), // Zechariah 2 has 13 verses
    3: Array.from({ length: 10 }, (_, i) => i + 1), // Zechariah 3 has 10 verses
    4: Array.from({ length: 14 }, (_, i) => i + 1), // Zechariah 4 has 14 verses
    5: Array.from({ length: 11 }, (_, i) => i + 1), // Zechariah 5 has 11 verses
    6: Array.from({ length: 15 }, (_, i) => i + 1), // Zechariah 6 has 15 verses
    7: Array.from({ length: 14 }, (_, i) => i + 1), // Zechariah 7 has 14 verses
    8: Array.from({ length: 23 }, (_, i) => i + 1), // Zechariah 8 has 23 verses
    9: Array.from({ length: 17 }, (_, i) => i + 1), // Zechariah 9 has 17 verses
    10: Array.from({ length: 12 }, (_, i) => i + 1), // Zechariah 10 has 12 verses
    11: Array.from({ length: 17 }, (_, i) => i + 1), // Zechariah 11 has 17 verses
    12: Array.from({ length: 14 }, (_, i) => i + 1), // Zechariah 12 has 14 verses
    13: Array.from({ length: 9 }, (_, i) => i + 1), // Zechariah 13 has 9 verses
    14: Array.from({ length: 21 }, (_, i) => i + 1), // Zechariah 14 has 21 verses
}
