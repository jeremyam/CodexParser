module.exports = {
    1: Array.from({ length: 26 }, (_, i) => i + 1), // Acts 1 has 26 verses
    2: Array.from({ length: 47 }, (_, i) => i + 1), // Acts 2 has 47 verses
    3: Array.from({ length: 26 }, (_, i) => i + 1), // Acts 3 has 26 verses
    4: Array.from({ length: 37 }, (_, i) => i + 1), // Acts 4 has 37 verses
    5: Array.from({ length: 42 }, (_, i) => i + 1), // Acts 5 has 42 verses
    6: Array.from({ length: 15 }, (_, i) => i + 1), // Acts 6 has 15 verses
    7: Array.from({ length: 60 }, (_, i) => i + 1), // Acts 7 has 60 verses
    8: Array.from({ length: 40 }, (_, i) => i + 1), // Acts 8 has 40 verses
    9: Array.from({ length: 43 }, (_, i) => i + 1), // Acts 9 has 43 verses
    10: Array.from({ length: 48 }, (_, i) => i + 1), // Acts 10 has 48 verses
    11: Array.from({ length: 30 }, (_, i) => i + 1), // Acts 11 has 30 verses
    12: Array.from({ length: 25 }, (_, i) => i + 1), // Acts 12 has 25 verses
    13: Array.from({ length: 52 }, (_, i) => i + 1), // Acts 13 has 52 verses
    14: Array.from({ length: 28 }, (_, i) => i + 1), // Acts 14 has 28 verses
    15: Array.from({ length: 41 }, (_, i) => i + 1), // Acts 15 has 41 verses
    16: Array.from({ length: 40 }, (_, i) => i + 1), // Acts 16 has 40 verses
    17: Array.from({ length: 34 }, (_, i) => i + 1), // Acts 17 has 34 verses
    18: Array.from({ length: 28 }, (_, i) => i + 1), // Acts 18 has 28 verses
    19: Array.from({ length: 41 }, (_, i) => i + 1), // Acts 19 has 41 verses
    20: Array.from({ length: 38 }, (_, i) => i + 1), // Acts 20 has 38 verses
    21: Array.from({ length: 40 }, (_, i) => i + 1), // Acts 21 has 40 verses
    22: Array.from({ length: 30 }, (_, i) => i + 1), // Acts 22 has 30 verses
    23: Array.from({ length: 35 }, (_, i) => i + 1), // Acts 23 has 35 verses
    24: Array.from({ length: 27 }, (_, i) => i + 1), // Acts 24 has 27 verses
    25: Array.from({ length: 27 }, (_, i) => i + 1), // Acts 25 has 27 verses
    26: Array.from({ length: 32 }, (_, i) => i + 1), // Acts 26 has 32 verses
    27: Array.from({ length: 44 }, (_, i) => i + 1), // Acts 27 has 44 verses
    28: Array.from({ length: 31 }, (_, i) => i + 1), // Acts 28 has 31 verses
}
