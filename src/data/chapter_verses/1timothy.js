module.exports = {
    1: Array.from({ length: 20 }, (_, i) => i + 1), // 1 Timothy 1 has 20 verses
    2: Array.from({ length: 15 }, (_, i) => i + 1), // 1 Timothy 2 has 15 verses
    3: Array.from({ length: 16 }, (_, i) => i + 1), // 1 Timothy 3 has 16 verses
    4: Array.from({ length: 16 }, (_, i) => i + 1), // 1 Timothy 4 has 16 verses
    5: Array.from({ length: 25 }, (_, i) => i + 1), // 1 Timothy 5 has 25 verses
    6: Array.from({ length: 21 }, (_, i) => i + 1), // 1 Timothy 6 has 21 verses
}
