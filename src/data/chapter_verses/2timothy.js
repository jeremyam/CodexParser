module.exports = {
    1: Array.from({ length: 18 }, (_, i) => i + 1), // 2 Timothy 1 has 18 verses
    2: Array.from({ length: 26 }, (_, i) => i + 1), // 2 Timothy 2 has 26 verses
    3: Array.from({ length: 17 }, (_, i) => i + 1), // 2 Timothy 3 has 17 verses
    4: Array.from({ length: 22 }, (_, i) => i + 1), // 2 Timothy 4 has 22 verses
}
