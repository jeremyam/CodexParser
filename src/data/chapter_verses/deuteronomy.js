module.exports = {
    1: Array.from({ length: 46 }, (_, i) => i + 1), // Deuteronomy 1 has 46 verses
    2: Array.from({ length: 37 }, (_, i) => i + 1), // Deuteronomy 2 has 37 verses
    3: Array.from({ length: 29 }, (_, i) => i + 1), // Deuteronomy 3 has 29 verses
    4: Array.from({ length: 49 }, (_, i) => i + 1), // Deuteronomy 4 has 49 verses
    5: Array.from({ length: 33 }, (_, i) => i + 1), // Deuteronomy 5 has 33 verses
    6: Array.from({ length: 25 }, (_, i) => i + 1), // Deuteronomy 6 has 25 verses
    7: Array.from({ length: 26 }, (_, i) => i + 1), // Deuteronomy 7 has 26 verses
    8: Array.from({ length: 20 }, (_, i) => i + 1), // Deuteronomy 8 has 20 verses
    9: Array.from({ length: 29 }, (_, i) => i + 1), // Deuteronomy 9 has 29 verses
    10: Array.from({ length: 22 }, (_, i) => i + 1), // Deuteronomy 10 has 22 verses
    11: Array.from({ length: 32 }, (_, i) => i + 1), // Deuteronomy 11 has 32 verses
    12: Array.from({ length: 32 }, (_, i) => i + 1), // Deuteronomy 12 has 32 verses
    13: Array.from({ length: 18 }, (_, i) => i + 1), // Deuteronomy 13 has 18 verses
    14: Array.from({ length: 29 }, (_, i) => i + 1), // Deuteronomy 14 has 29 verses
    15: Array.from({ length: 23 }, (_, i) => i + 1), // Deuteronomy 15 has 23 verses
    16: Array.from({ length: 22 }, (_, i) => i + 1), // Deuteronomy 16 has 22 verses
    17: Array.from({ length: 20 }, (_, i) => i + 1), // Deuteronomy 17 has 20 verses
    18: Array.from({ length: 22 }, (_, i) => i + 1), // Deuteronomy 18 has 22 verses
    19: Array.from({ length: 21 }, (_, i) => i + 1), // Deuteronomy 19 has 21 verses
    20: Array.from({ length: 20 }, (_, i) => i + 1), // Deuteronomy 20 has 20 verses
    21: Array.from({ length: 23 }, (_, i) => i + 1), // Deuteronomy 21 has 23 verses
    22: Array.from({ length: 30 }, (_, i) => i + 1), // Deuteronomy 22 has 30 verses
    23: Array.from({ length: 25 }, (_, i) => i + 1), // Deuteronomy 23 has 25 verses
    24: Array.from({ length: 22 }, (_, i) => i + 1), // Deuteronomy 24 has 22 verses
    25: Array.from({ length: 19 }, (_, i) => i + 1), // Deuteronomy 25 has 19 verses
    26: Array.from({ length: 19 }, (_, i) => i + 1), // Deuteronomy 26 has 19 verses
    27: Array.from({ length: 26 }, (_, i) => i + 1), // Deuteronomy 27 has 26 verses
    28: Array.from({ length: 68 }, (_, i) => i + 1), // Deuteronomy 28 has 68 verses
    29: Array.from({ length: 29 }, (_, i) => i + 1), // Deuteronomy 29 has 29 verses
    30: Array.from({ length: 20 }, (_, i) => i + 1), // Deuteronomy 30 has 20 verses
    31: Array.from({ length: 30 }, (_, i) => i + 1), // Deuteronomy 31 has 30 verses
    32: Array.from({ length: 52 }, (_, i) => i + 1), // Deuteronomy 32 has 52 verses
    33: Array.from({ length: 29 }, (_, i) => i + 1), // Deuteronomy 33 has 29 verses
    34: Array.from({ length: 12 }, (_, i) => i + 1), // Deuteronomy 34 has 12 verses
}
