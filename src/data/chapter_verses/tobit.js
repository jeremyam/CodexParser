module.exports = {
    1: Array.from({ length: 22 }, (_, i) => i + 1), // Tobit 1 has 22 verses
    2: Array.from({ length: 14 }, (_, i) => i + 1), // Tobit 2 has 14 verses
    3: Array.from({ length: 17 }, (_, i) => i + 1), // Tobit 3 has 17 verses
    4: Array.from({ length: 21 }, (_, i) => i + 1), // Tobit 4 has 21 verses
    5: Array.from({ length: 23 }, (_, i) => i + 1), // Tobit 5 has 23 verses
    6: Array.from({ length: 19 }, (_, i) => i + 1), // Tobit 6 has 19 verses
    7: Array.from({ length: 18 }, (_, i) => i + 1), // Tobit 7 has 18 verses
    8: Array.from({ length: 21 }, (_, i) => i + 1), // Tobit 8 has 21 verses
    9: Array.from({ length: 6 }, (_, i) => i + 1), // Tobit 9 has 6 verses
    10: Array.from({ length: 14 }, (_, i) => i + 1), // Tobit 10 has 14 verses
    11: Array.from({ length: 19 }, (_, i) => i + 1), // Tobit 11 has 19 verses
    12: Array.from({ length: 22 }, (_, i) => i + 1), // Tobit 12 has 22 verses
    13: Array.from({ length: 19 }, (_, i) => i + 1), // Tobit 13 has 19 verses
    14: Array.from({ length: 15 }, (_, i) => i + 1), // Tobit 14 has 15 verses
}
