module.exports = {
    1: Array.from({ length: 17 }, (_, i) => i + 1), // Song of Songs 1 has 17 verses
    2: Array.from({ length: 17 }, (_, i) => i + 1), // Song of Songs 2 has 17 verses
    3: Array.from({ length: 11 }, (_, i) => i + 1), // Song of Songs 3 has 11 verses
    4: Array.from({ length: 16 }, (_, i) => i + 1), // Song of Songs 4 has 16 verses
    5: Array.from({ length: 16 }, (_, i) => i + 1), // Song of Songs 5 has 16 verses
    6: Array.from({ length: 13 }, (_, i) => i + 1), // Song of Songs 6 has 13 verses
    7: Array.from({ length: 13 }, (_, i) => i + 1), // Song of Songs 7 has 13 verses
    8: Array.from({ length: 14 }, (_, i) => i + 1), // Song of Songs 8 has 14 verses
}
