module.exports = {
    1: Array.from({ length: 11 }, (_, i) => i + 1), // Nehemiah 1 has 11 verses
    2: Array.from({ length: 20 }, (_, i) => i + 1), // Nehemiah 2 has 20 verses
    3: Array.from({ length: 32 }, (_, i) => i + 1), // Nehemiah 3 has 32 verses
    4: Array.from({ length: 23 }, (_, i) => i + 1), // Nehemiah 4 has 23 verses
    5: Array.from({ length: 19 }, (_, i) => i + 1), // Nehemiah 5 has 19 verses
    6: Array.from({ length: 19 }, (_, i) => i + 1), // Nehemiah 6 has 19 verses
    7: Array.from({ length: 73 }, (_, i) => i + 1), // Nehemiah 7 has 73 verses
    8: Array.from({ length: 18 }, (_, i) => i + 1), // Nehemiah 8 has 18 verses
    9: Array.from({ length: 38 }, (_, i) => i + 1), // Nehemiah 9 has 38 verses
    10: Array.from({ length: 39 }, (_, i) => i + 1), // Nehemiah 10 has 39 verses
    11: Array.from({ length: 36 }, (_, i) => i + 1), // Nehemiah 11 has 36 verses
    12: Array.from({ length: 47 }, (_, i) => i + 1), // Nehemiah 12 has 47 verses
    13: Array.from({ length: 31 }, (_, i) => i + 1), // Nehemiah 13 has 31 verses
}
