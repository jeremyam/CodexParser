module.exports = {
    1: Array.from({ length: 28 }, (_, i) => i + 1), // 1 Samuel 1 has 28 verses
    2: Array.from({ length: 36 }, (_, i) => i + 1), // 1 Samuel 2 has 36 verses
    3: Array.from({ length: 21 }, (_, i) => i + 1), // 1 Samuel 3 has 21 verses
    4: Array.from({ length: 22 }, (_, i) => i + 1), // 1 Samuel 4 has 22 verses
    5: Array.from({ length: 12 }, (_, i) => i + 1), // 1 Samuel 5 has 12 verses
    6: Array.from({ length: 21 }, (_, i) => i + 1), // 1 Samuel 6 has 21 verses
    7: Array.from({ length: 17 }, (_, i) => i + 1), // 1 Samuel 7 has 17 verses
    8: Array.from({ length: 22 }, (_, i) => i + 1), // 1 Samuel 8 has 22 verses
    9: Array.from({ length: 27 }, (_, i) => i + 1), // 1 Samuel 9 has 27 verses
    10: Array.from({ length: 27 }, (_, i) => i + 1), // 1 Samuel 10 has 27 verses
    11: Array.from({ length: 15 }, (_, i) => i + 1), // 1 Samuel 11 has 15 verses
    12: Array.from({ length: 25 }, (_, i) => i + 1), // 1 Samuel 12 has 25 verses
    13: Array.from({ length: 23 }, (_, i) => i + 1), // 1 Samuel 13 has 23 verses
    14: Array.from({ length: 52 }, (_, i) => i + 1), // 1 Samuel 14 has 52 verses
    15: Array.from({ length: 35 }, (_, i) => i + 1), // 1 Samuel 15 has 35 verses
    16: Array.from({ length: 23 }, (_, i) => i + 1), // 1 Samuel 16 has 23 verses
    17: Array.from({ length: 58 }, (_, i) => i + 1), // 1 Samuel 17 has 58 verses
    18: Array.from({ length: 30 }, (_, i) => i + 1), // 1 Samuel 18 has 30 verses
    19: Array.from({ length: 24 }, (_, i) => i + 1), // 1 Samuel 19 has 24 verses
    20: Array.from({ length: 42 }, (_, i) => i + 1), // 1 Samuel 20 has 42 verses
    21: Array.from({ length: 15 }, (_, i) => i + 1), // 1 Samuel 21 has 15 verses
    22: Array.from({ length: 23 }, (_, i) => i + 1), // 1 Samuel 22 has 23 verses
    23: Array.from({ length: 29 }, (_, i) => i + 1), // 1 Samuel 23 has 29 verses
    24: Array.from({ length: 22 }, (_, i) => i + 1), // 1 Samuel 24 has 22 verses
    25: Array.from({ length: 44 }, (_, i) => i + 1), // 1 Samuel 25 has 44 verses
    26: Array.from({ length: 25 }, (_, i) => i + 1), // 1 Samuel 26 has 25 verses
    27: Array.from({ length: 12 }, (_, i) => i + 1), // 1 Samuel 27 has 12 verses
    28: Array.from({ length: 25 }, (_, i) => i + 1), // 1 Samuel 28 has 25 verses
    29: Array.from({ length: 11 }, (_, i) => i + 1), // 1 Samuel 29 has 11 verses
    30: Array.from({ length: 31 }, (_, i) => i + 1), // 1 Samuel 30 has 31 verses
    31: Array.from({ length: 13 }, (_, i) => i + 1), // 1 Samuel 31 has 13 verses
}
