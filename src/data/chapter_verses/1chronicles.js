module.exports = {
    1: Array.from({ length: 54 }, (_, i) => i + 1), // 1 Chronicles 1 has 54 verses
    2: Array.from({ length: 55 }, (_, i) => i + 1), // 1 Chronicles 2 has 55 verses
    3: Array.from({ length: 24 }, (_, i) => i + 1), // 1 Chronicles 3 has 24 verses
    4: Array.from({ length: 43 }, (_, i) => i + 1), // 1 Chronicles 4 has 43 verses
    5: Array.from({ length: 26 }, (_, i) => i + 1), // 1 Chronicles 5 has 26 verses
    6: Array.from({ length: 81 }, (_, i) => i + 1), // 1 Chronicles 6 has 81 verses
    7: Array.from({ length: 40 }, (_, i) => i + 1), // 1 Chronicles 7 has 40 verses
    8: Array.from({ length: 40 }, (_, i) => i + 1), // 1 Chronicles 8 has 40 verses
    9: Array.from({ length: 44 }, (_, i) => i + 1), // 1 Chronicles 9 has 44 verses
    10: Array.from({ length: 14 }, (_, i) => i + 1), // 1 Chronicles 10 has 14 verses
    11: Array.from({ length: 47 }, (_, i) => i + 1), // 1 Chronicles 11 has 47 verses
    12: Array.from({ length: 40 }, (_, i) => i + 1), // 1 Chronicles 12 has 40 verses
    13: Array.from({ length: 14 }, (_, i) => i + 1), // 1 Chronicles 13 has 14 verses
    14: Array.from({ length: 17 }, (_, i) => i + 1), // 1 Chronicles 14 has 17 verses
    15: Array.from({ length: 29 }, (_, i) => i + 1), // 1 Chronicles 15 has 29 verses
    16: Array.from({ length: 43 }, (_, i) => i + 1), // 1 Chronicles 16 has 43 verses
    17: Array.from({ length: 27 }, (_, i) => i + 1), // 1 Chronicles 17 has 27 verses
    18: Array.from({ length: 17 }, (_, i) => i + 1), // 1 Chronicles 18 has 17 verses
    19: Array.from({ length: 19 }, (_, i) => i + 1), // 1 Chronicles 19 has 19 verses
    20: Array.from({ length: 8 }, (_, i) => i + 1), // 1 Chronicles 20 has 8 verses
    21: Array.from({ length: 30 }, (_, i) => i + 1), // 1 Chronicles 21 has 30 verses
    22: Array.from({ length: 19 }, (_, i) => i + 1), // 1 Chronicles 22 has 19 verses
    23: Array.from({ length: 32 }, (_, i) => i + 1), // 1 Chronicles 23 has 32 verses
    24: Array.from({ length: 31 }, (_, i) => i + 1), // 1 Chronicles 24 has 31 verses
    25: Array.from({ length: 31 }, (_, i) => i + 1), // 1 Chronicles 25 has 31 verses
    26: Array.from({ length: 32 }, (_, i) => i + 1), // 1 Chronicles 26 has 32 verses
    27: Array.from({ length: 34 }, (_, i) => i + 1), // 1 Chronicles 27 has 34 verses
    28: Array.from({ length: 21 }, (_, i) => i + 1), // 1 Chronicles 28 has 21 verses
    29: Array.from({ length: 30 }, (_, i) => i + 1), // 1 Chronicles 29 has 30 verses
}
