module.exports = {
    1: Array.from({ length: 6 }, (_, i) => i + 1), // Psalm 1 has 6 verses
    2: Array.from({ length: 12 }, (_, i) => i + 1), // Psalm 2 has 12 verses
    3: Array.from({ length: 8 }, (_, i) => i + 1), // Psalm 3 has 8 verses
    4: Array.from({ length: 8 }, (_, i) => i + 1), // Psalm 4 has 8 verses
    5: Array.from({ length: 12 }, (_, i) => i + 1), // Psalm 5 has 12 verses
    6: Array.from({ length: 10 }, (_, i) => i + 1), // Psalm 6 has 10 verses
    7: Array.from({ length: 17 }, (_, i) => i + 1), // Psalm 7 has 17 verses
    8: Array.from({ length: 9 }, (_, i) => i + 1), // Psalm 8 has 9 verses
    9: Array.from({ length: 20 }, (_, i) => i + 1), // Psalm 9 has 20 verses
    10: Array.from({ length: 18 }, (_, i) => i + 1), // Psalm 10 has 18 verses
    11: Array.from({ length: 7 }, (_, i) => i + 1), // Psalm 11 has 7 verses
    12: Array.from({ length: 8 }, (_, i) => i + 1), // Psalm 12 has 8 verses
    13: Array.from({ length: 6 }, (_, i) => i + 1), // Psalm 13 has 6 verses
    14: Array.from({ length: 7 }, (_, i) => i + 1), // Psalm 14 has 7 verses
    15: Array.from({ length: 5 }, (_, i) => i + 1), // Psalm 15 has 5 verses
    16: Array.from({ length: 11 }, (_, i) => i + 1), // Psalm 16 has 11 verses
    17: Array.from({ length: 15 }, (_, i) => i + 1), // Psalm 17 has 15 verses
    18: Array.from({ length: 50 }, (_, i) => i + 1), // Psalm 18 has 50 verses
    19: Array.from({ length: 14 }, (_, i) => i + 1), // Psalm 19 has 14 verses
    20: Array.from({ length: 9 }, (_, i) => i + 1), // Psalm 20 has 9 verses
    21: Array.from({ length: 13 }, (_, i) => i + 1), // Psalm 21 has 13 verses
    22: Array.from({ length: 31 }, (_, i) => i + 1), // Psalm 22 has 31 verses
    23: Array.from({ length: 6 }, (_, i) => i + 1), // Psalm 23 has 6 verses
    24: Array.from({ length: 10 }, (_, i) => i + 1), // Psalm 24 has 10 verses
    25: Array.from({ length: 22 }, (_, i) => i + 1), // Psalm 25 has 22 verses
    26: Array.from({ length: 12 }, (_, i) => i + 1), // Psalm 26 has 12 verses
    27: Array.from({ length: 14 }, (_, i) => i + 1), // Psalm 27 has 14 verses
    28: Array.from({ length: 9 }, (_, i) => i + 1), // Psalm 28 has 9 verses
    29: Array.from({ length: 11 }, (_, i) => i + 1), // Psalm 29 has 11 verses
    30: Array.from({ length: 12 }, (_, i) => i + 1), // Psalm 30 has 12 verses
    31: Array.from({ length: 24 }, (_, i) => i + 1), // Psalm 31 has 24 verses
    32: Array.from({ length: 11 }, (_, i) => i + 1), // Psalm 32 has 11 verses
    33: Array.from({ length: 22 }, (_, i) => i + 1), // Psalm 33 has 22 verses
    34: Array.from({ length: 22 }, (_, i) => i + 1), // Psalm 34 has 22 verses
    35: Array.from({ length: 28 }, (_, i) => i + 1), // Psalm 35 has 28 verses
    36: Array.from({ length: 12 }, (_, i) => i + 1), // Psalm 36 has 12 verses
    37: Array.from({ length: 40 }, (_, i) => i + 1), // Psalm 37 has 40 verses
    38: Array.from({ length: 22 }, (_, i) => i + 1), // Psalm 38 has 22 verses
    39: Array.from({ length: 13 }, (_, i) => i + 1), // Psalm 39 has 13 verses
    40: Array.from({ length: 17 }, (_, i) => i + 1), // Psalm 40 has 17 verses
    41: Array.from({ length: 13 }, (_, i) => i + 1), // Psalm 41 has 13 verses
    42: Array.from({ length: 11 }, (_, i) => i + 1), // Psalm 42 has 11 verses
    43: Array.from({ length: 5 }, (_, i) => i + 1), // Psalm 43 has 5 verses
    44: Array.from({ length: 26 }, (_, i) => i + 1), // Psalm 44 has 26 verses
    45: Array.from({ length: 17 }, (_, i) => i + 1), // Psalm 45 has 17 verses
    46: Array.from({ length: 11 }, (_, i) => i + 1), // Psalm 46 has 11 verses
    47: Array.from({ length: 9 }, (_, i) => i + 1), // Psalm 47 has 9 verses
    48: Array.from({ length: 14 }, (_, i) => i + 1), // Psalm 48 has 14 verses
    49: Array.from({ length: 20 }, (_, i) => i + 1), // Psalm 49 has 20 verses
    50: Array.from({ length: 23 }, (_, i) => i + 1), // Psalm 50 has 23 verses
    51: Array.from({ length: 19 }, (_, i) => i + 1), // Psalm 51 has 19 verses
    52: Array.from({ length: 9 }, (_, i) => i + 1), // Psalm 52 has 9 verses
    53: Array.from({ length: 6 }, (_, i) => i + 1), // Psalm 53 has 6 verses
    54: Array.from({ length: 7 }, (_, i) => i + 1), // Psalm 54 has 7 verses
    55: Array.from({ length: 23 }, (_, i) => i + 1), // Psalm 55 has 23 verses
    56: Array.from({ length: 13 }, (_, i) => i + 1), // Psalm 56 has 13 verses
    57: Array.from({ length: 11 }, (_, i) => i + 1), // Psalm 57 has 11 verses
    58: Array.from({ length: 11 }, (_, i) => i + 1), // Psalm 58 has 11 verses
    59: Array.from({ length: 17 }, (_, i) => i + 1), // Psalm 59 has 17 verses
    60: Array.from({ length: 12 }, (_, i) => i + 1), // Psalm 60 has 12 verses
    61: Array.from({ length: 8 }, (_, i) => i + 1), // Psalm 61 has 8 verses
    62: Array.from({ length: 12 }, (_, i) => i + 1), // Psalm 62 has 12 verses
    63: Array.from({ length: 11 }, (_, i) => i + 1), // Psalm 63 has 11 verses
    64: Array.from({ length: 10 }, (_, i) => i + 1), // Psalm 64 has 10 verses
    65: Array.from({ length: 13 }, (_, i) => i + 1), // Psalm 65 has 13 verses
    66: Array.from({ length: 20 }, (_, i) => i + 1), // Psalm 66 has 20 verses
    67: Array.from({ length: 7 }, (_, i) => i + 1), // Psalm 67 has 7 verses
    68: Array.from({ length: 35 }, (_, i) => i + 1), // Psalm 68 has 35 verses
    69: Array.from({ length: 36 }, (_, i) => i + 1), // Psalm 69 has 36 verses
    70: Array.from({ length: 5 }, (_, i) => i + 1), // Psalm 70 has 5 verses
    71: Array.from({ length: 24 }, (_, i) => i + 1), // Psalm 71 has 24 verses
    72: Array.from({ length: 20 }, (_, i) => i + 1), // Psalm 72 has 20 verses
    73: Array.from({ length: 28 }, (_, i) => i + 1), // Psalm 73 has 28 verses
    74: Array.from({ length: 23 }, (_, i) => i + 1), // Psalm 74 has 23 verses
    75: Array.from({ length: 10 }, (_, i) => i + 1), // Psalm 75 has 10 verses
    76: Array.from({ length: 12 }, (_, i) => i + 1), // Psalm 76 has 12 verses
    77: Array.from({ length: 20 }, (_, i) => i + 1), // Psalm 77 has 20 verses
    78: Array.from({ length: 72 }, (_, i) => i + 1), // Psalm 78 has 72 verses
    79: Array.from({ length: 13 }, (_, i) => i + 1), // Psalm 79 has 13 verses
    80: Array.from({ length: 19 }, (_, i) => i + 1), // Psalm 80 has 19 verses
    81: Array.from({ length: 16 }, (_, i) => i + 1), // Psalm 81 has 16 verses
    82: Array.from({ length: 8 }, (_, i) => i + 1), // Psalm 82 has 8 verses
    83: Array.from({ length: 18 }, (_, i) => i + 1), // Psalm 83 has 18 verses
    84: Array.from({ length: 12 }, (_, i) => i + 1), // Psalm 84 has 12 verses
    85: Array.from({ length: 13 }, (_, i) => i + 1), // Psalm 85 has 13 verses
    86: Array.from({ length: 17 }, (_, i) => i + 1), // Psalm 86 has 17 verses
    87: Array.from({ length: 7 }, (_, i) => i + 1), // Psalm 87 has 7 verses
    88: Array.from({ length: 18 }, (_, i) => i + 1), // Psalm 88 has 18 verses
    89: Array.from({ length: 52 }, (_, i) => i + 1), // Psalm 89 has 52 verses
    90: Array.from({ length: 17 }, (_, i) => i + 1), // Psalm 90 has 17 verses
    91: Array.from({ length: 16 }, (_, i) => i + 1), // Psalm 91 has 16 verses
    92: Array.from({ length: 15 }, (_, i) => i + 1), // Psalm 92 has 15 verses
    93: Array.from({ length: 5 }, (_, i) => i + 1), // Psalm 93 has 5 verses
    94: Array.from({ length: 23 }, (_, i) => i + 1), // Psalm 94 has 23 verses
    95: Array.from({ length: 11 }, (_, i) => i + 1), // Psalm 95 has 11 verses
    96: Array.from({ length: 13 }, (_, i) => i + 1), // Psalm 96 has 13 verses
    97: Array.from({ length: 12 }, (_, i) => i + 1), // Psalm 97 has 12 verses
    98: Array.from({ length: 9 }, (_, i) => i + 1), // Psalm 98 has 9 verses
    99: Array.from({ length: 9 }, (_, i) => i + 1), // Psalm 99 has 9 verses
    100: Array.from({ length: 5 }, (_, i) => i + 1), // Psalm 100 has 5 verses
    101: Array.from({ length: 8 }, (_, i) => i + 1), // Psalm 101 has 8 verses
    102: Array.from({ length: 28 }, (_, i) => i + 1), // Psalm 102 has 28 verses
    103: Array.from({ length: 22 }, (_, i) => i + 1), // Psalm 103 has 22 verses
    104: Array.from({ length: 35 }, (_, i) => i + 1), // Psalm 104 has 35 verses
    105: Array.from({ length: 45 }, (_, i) => i + 1), // Psalm 105 has 45 verses
    106: Array.from({ length: 48 }, (_, i) => i + 1), // Psalm 106 has 48 verses
    107: Array.from({ length: 43 }, (_, i) => i + 1), // Psalm 107 has 43 verses
    108: Array.from({ length: 13 }, (_, i) => i + 1), // Psalm 108 has 13 verses
    109: Array.from({ length: 31 }, (_, i) => i + 1), // Psalm 109 has 31 verses
    110: Array.from({ length: 7 }, (_, i) => i + 1), // Psalm 110 has 7 verses
    111: Array.from({ length: 10 }, (_, i) => i + 1), // Psalm 111 has 10 verses
    112: Array.from({ length: 10 }, (_, i) => i + 1), // Psalm 112 has 10 verses
    113: Array.from({ length: 9 }, (_, i) => i + 1), // Psalm 113 has 9 verses
    114: Array.from({ length: 8 }, (_, i) => i + 1), // Psalm 114 has 8 verses
    115: Array.from({ length: 18 }, (_, i) => i + 1), // Psalm 115 has 18 verses
    116: Array.from({ length: 19 }, (_, i) => i + 1), // Psalm 116 has 19 verses
    117: Array.from({ length: 2 }, (_, i) => i + 1), // Psalm 117 has 2 verses
    118: Array.from({ length: 29 }, (_, i) => i + 1), // Psalm 118 has 29 verses
    119: Array.from({ length: 176 }, (_, i) => i + 1), // Psalm 119 has 176 verses
    120: Array.from({ length: 7 }, (_, i) => i + 1), // Psalm 120 has 7 verses
    121: Array.from({ length: 8 }, (_, i) => i + 1), // Psalm 121 has 8 verses
    122: Array.from({ length: 9 }, (_, i) => i + 1), // Psalm 122 has 9 verses
    123: Array.from({ length: 4 }, (_, i) => i + 1), // Psalm 123 has 4 verses
    124: Array.from({ length: 8 }, (_, i) => i + 1), // Psalm 124 has 8 verses
    125: Array.from({ length: 5 }, (_, i) => i + 1), // Psalm 125 has 5 verses
    126: Array.from({ length: 6 }, (_, i) => i + 1), // Psalm 126 has 6 verses
    127: Array.from({ length: 5 }, (_, i) => i + 1), // Psalm 127 has 5 verses
    128: Array.from({ length: 6 }, (_, i) => i + 1), // Psalm 128 has 6 verses
    129: Array.from({ length: 8 }, (_, i) => i + 1), // Psalm 129 has 8 verses
    130: Array.from({ length: 8 }, (_, i) => i + 1), // Psalm 130 has 8 verses
    131: Array.from({ length: 3 }, (_, i) => i + 1), // Psalm 131 has 3 verses
    132: Array.from({ length: 18 }, (_, i) => i + 1), // Psalm 132 has 18 verses
    133: Array.from({ length: 3 }, (_, i) => i + 1), // Psalm 133 has 3 verses
    134: Array.from({ length: 3 }, (_, i) => i + 1), // Psalm 134 has 3 verses
    135: Array.from({ length: 21 }, (_, i) => i + 1), // Psalm 135 has 21 verses
    136: Array.from({ length: 26 }, (_, i) => i + 1), // Psalm 136 has 26 verses
    137: Array.from({ length: 9 }, (_, i) => i + 1), // Psalm 137 has 9 verses
    138: Array.from({ length: 8 }, (_, i) => i + 1), // Psalm 138 has 8 verses
    139: Array.from({ length: 24 }, (_, i) => i + 1), // Psalm 139 has 24 verses
    140: Array.from({ length: 13 }, (_, i) => i + 1), // Psalm 140 has 13 verses
    141: Array.from({ length: 10 }, (_, i) => i + 1), // Psalm 141 has 10 verses
    142: Array.from({ length: 7 }, (_, i) => i + 1), // Psalm 142 has 7 verses
    143: Array.from({ length: 12 }, (_, i) => i + 1), // Psalm 143 has 12 verses
    144: Array.from({ length: 15 }, (_, i) => i + 1), // Psalm 144 has 15 verses
    145: Array.from({ length: 21 }, (_, i) => i + 1), // Psalm 145 has 21 verses
    146: Array.from({ length: 10 }, (_, i) => i + 1), // Psalm 146 has 10 verses
    147: Array.from({ length: 20 }, (_, i) => i + 1), // Psalm 147 has 20 verses
    148: Array.from({ length: 14 }, (_, i) => i + 1), // Psalm 148 has 14 verses
    149: Array.from({ length: 9 }, (_, i) => i + 1), // Psalm 149 has 9 verses
    150: Array.from({ length: 6 }, (_, i) => i + 1), // Psalm 150 has 6 verses
}
