module.exports = {
    1: Array.from({ length: 16 }, (_, i) => i + 1), // Micah 1 has 16 verses
    2: Array.from({ length: 13 }, (_, i) => i + 1), // Micah 2 has 13 verses
    3: Array.from({ length: 12 }, (_, i) => i + 1), // Micah 3 has 12 verses
    4: Array.from({ length: 13 }, (_, i) => i + 1), // Micah 4 has 13 verses
    5: Array.from({ length: 15 }, (_, i) => i + 1), // Micah 5 has 15 verses
    6: Array.from({ length: 16 }, (_, i) => i + 1), // Micah 6 has 16 verses
    7: Array.from({ length: 20 }, (_, i) => i + 1), // Micah 7 has 20 verses
}
