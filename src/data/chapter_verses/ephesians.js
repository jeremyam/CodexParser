module.exports = {
    1: Array.from({ length: 23 }, (_, i) => i + 1), // Ephesians 1 has 23 verses
    2: Array.from({ length: 22 }, (_, i) => i + 1), // Ephesians 2 has 22 verses
    3: Array.from({ length: 21 }, (_, i) => i + 1), // Ephesians 3 has 21 verses
    4: Array.from({ length: 32 }, (_, i) => i + 1), // Ephesians 4 has 32 verses
    5: Array.from({ length: 33 }, (_, i) => i + 1), // Ephesians 5 has 33 verses
    6: Array.from({ length: 24 }, (_, i) => i + 1), // Ephesians 6 has 24 verses
}
