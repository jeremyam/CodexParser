module.exports = {
    1: Array.from({ length: 29 }, (_, i) => i + 1), // Colossians 1 has 29 verses
    2: Array.from({ length: 23 }, (_, i) => i + 1), // Colossians 2 has 23 verses
    3: Array.from({ length: 25 }, (_, i) => i + 1), // Colossians 3 has 25 verses
    4: Array.from({ length: 18 }, (_, i) => i + 1), // Colossians 4 has 18 verses
}
