module.exports = {
    1: Array.from({ length: 45 }, (_, i) => i + 1), // Mark 1 has 45 verses
    2: Array.from({ length: 28 }, (_, i) => i + 1), // Mark 2 has 28 verses
    3: Array.from({ length: 35 }, (_, i) => i + 1), // Mark 3 has 35 verses
    4: Array.from({ length: 41 }, (_, i) => i + 1), // Mark 4 has 41 verses
    5: Array.from({ length: 43 }, (_, i) => i + 1), // Mark 5 has 43 verses
    6: Array.from({ length: 56 }, (_, i) => i + 1), // Mark 6 has 56 verses
    7: Array.from({ length: 37 }, (_, i) => i + 1), // Mark 7 has 37 verses
    8: Array.from({ length: 38 }, (_, i) => i + 1), // Mark 8 has 38 verses
    9: Array.from({ length: 50 }, (_, i) => i + 1), // Mark 9 has 50 verses
    10: Array.from({ length: 52 }, (_, i) => i + 1), // Mark 10 has 52 verses
    11: Array.from({ length: 33 }, (_, i) => i + 1), // Mark 11 has 33 verses
    12: Array.from({ length: 44 }, (_, i) => i + 1), // Mark 12 has 44 verses
    13: Array.from({ length: 37 }, (_, i) => i + 1), // Mark 13 has 37 verses
    14: Array.from({ length: 72 }, (_, i) => i + 1), // Mark 14 has 72 verses
    15: Array.from({ length: 47 }, (_, i) => i + 1), // Mark 15 has 47 verses
    16: Array.from({ length: 20 }, (_, i) => i + 1), // Mark 16 has 20 verses
}
