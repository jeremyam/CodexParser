module.exports = {
    1: Array.from({ length: 11 }, (_, i) => i + 1), // Ezra 1 has 11 verses
    2: Array.from({ length: 70 }, (_, i) => i + 1), // Ezra 2 has 70 verses
    3: Array.from({ length: 13 }, (_, i) => i + 1), // Ezra 3 has 13 verses
    4: Array.from({ length: 24 }, (_, i) => i + 1), // Ezra 4 has 24 verses
    5: Array.from({ length: 17 }, (_, i) => i + 1), // Ezra 5 has 17 verses
    6: Array.from({ length: 22 }, (_, i) => i + 1), // Ezra 6 has 22 verses
    7: Array.from({ length: 28 }, (_, i) => i + 1), // Ezra 7 has 28 verses
    8: Array.from({ length: 36 }, (_, i) => i + 1), // Ezra 8 has 36 verses
    9: Array.from({ length: 15 }, (_, i) => i + 1), // Ezra 9 has 15 verses
    10: Array.from({ length: 44 }, (_, i) => i + 1), // Ezra 10 has 44 verses
}
