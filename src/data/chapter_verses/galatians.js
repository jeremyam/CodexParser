module.exports = {
    1: Array.from({ length: 24 }, (_, i) => i + 1), // Galatians 1 has 24 verses
    2: Array.from({ length: 21 }, (_, i) => i + 1), // Galatians 2 has 21 verses
    3: Array.from({ length: 29 }, (_, i) => i + 1), // Galatians 3 has 29 verses
    4: Array.from({ length: 31 }, (_, i) => i + 1), // Galatians 4 has 31 verses
    5: Array.from({ length: 26 }, (_, i) => i + 1), // Galatians 5 has 26 verses
    6: Array.from({ length: 18 }, (_, i) => i + 1), // Galatians 6 has 18 verses
}
