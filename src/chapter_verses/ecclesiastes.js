module.exports = {
    1: Array.from({ length: 18 }, (_, i) => i + 1), // Ecclesiastes 1 has 18 verses
    2: Array.from({ length: 26 }, (_, i) => i + 1), // Ecclesiastes 2 has 26 verses
    3: Array.from({ length: 22 }, (_, i) => i + 1), // Ecclesiastes 3 has 22 verses
    4: Array.from({ length: 16 }, (_, i) => i + 1), // Ecclesiastes 4 has 16 verses
    5: Array.from({ length: 20 }, (_, i) => i + 1), // Ecclesiastes 5 has 20 verses
    6: Array.from({ length: 12 }, (_, i) => i + 1), // Ecclesiastes 6 has 12 verses
    7: Array.from({ length: 29 }, (_, i) => i + 1), // Ecclesiastes 7 has 29 verses
    8: Array.from({ length: 17 }, (_, i) => i + 1), // Ecclesiastes 8 has 17 verses
    9: Array.from({ length: 18 }, (_, i) => i + 1), // Ecclesiastes 9 has 18 verses
    10: Array.from({ length: 20 }, (_, i) => i + 1), // Ecclesiastes 10 has 20 verses
    11: Array.from({ length: 10 }, (_, i) => i + 1), // Ecclesiastes 11 has 10 verses
    12: Array.from({ length: 14 }, (_, i) => i + 1), // Ecclesiastes 12 has 14 verses
}
