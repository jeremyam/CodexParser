module.exports = {
    1: Array.from({ length: 21 }, (_, i) => i + 1), // 2 Peter 1 has 21 verses
    2: Array.from({ length: 22 }, (_, i) => i + 1), // 2 Peter 2 has 22 verses
    3: Array.from({ length: 18 }, (_, i) => i + 1), // 2 Peter 3 has 18 verses
}
