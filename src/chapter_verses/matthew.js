module.exports = {
    1: Array.from({ length: 25 }, (_, i) => i + 1), // Matthew 1 has 25 verses
    2: Array.from({ length: 23 }, (_, i) => i + 1), // Matthew 2 has 23 verses
    3: Array.from({ length: 17 }, (_, i) => i + 1), // Matthew 3 has 17 verses
    4: Array.from({ length: 25 }, (_, i) => i + 1), // Matthew 4 has 25 verses
    5: Array.from({ length: 48 }, (_, i) => i + 1), // Matthew 5 has 48 verses
    6: Array.from({ length: 34 }, (_, i) => i + 1), // Matthew 6 has 34 verses
    7: Array.from({ length: 29 }, (_, i) => i + 1), // Matthew 7 has 29 verses
    8: Array.from({ length: 34 }, (_, i) => i + 1), // Matthew 8 has 34 verses
    9: Array.from({ length: 38 }, (_, i) => i + 1), // Matthew 9 has 38 verses
    10: Array.from({ length: 42 }, (_, i) => i + 1), // Matthew 10 has 42 verses
    11: Array.from({ length: 30 }, (_, i) => i + 1), // Matthew 11 has 30 verses
    12: Array.from({ length: 50 }, (_, i) => i + 1), // Matthew 12 has 50 verses
    13: Array.from({ length: 58 }, (_, i) => i + 1), // Matthew 13 has 58 verses
    14: Array.from({ length: 36 }, (_, i) => i + 1), // Matthew 14 has 36 verses
    15: Array.from({ length: 39 }, (_, i) => i + 1), // Matthew 15 has 39 verses
    16: Array.from({ length: 28 }, (_, i) => i + 1), // Matthew 16 has 28 verses
    17: Array.from({ length: 27 }, (_, i) => i + 1), // Matthew 17 has 27 verses
    18: Array.from({ length: 35 }, (_, i) => i + 1), // Matthew 18 has 35 verses
    19: Array.from({ length: 30 }, (_, i) => i + 1), // Matthew 19 has 30 verses
    20: Array.from({ length: 34 }, (_, i) => i + 1), // Matthew 20 has 34 verses
    21: Array.from({ length: 46 }, (_, i) => i + 1), // Matthew 21 has 46 verses
    22: Array.from({ length: 46 }, (_, i) => i + 1), // Matthew 22 has 46 verses
    23: Array.from({ length: 39 }, (_, i) => i + 1), // Matthew 23 has 39 verses
    24: Array.from({ length: 51 }, (_, i) => i + 1), // Matthew 24 has 51 verses
    25: Array.from({ length: 46 }, (_, i) => i + 1), // Matthew 25 has 46 verses
    26: Array.from({ length: 75 }, (_, i) => i + 1), // Matthew 26 has 75 verses
    27: Array.from({ length: 66 }, (_, i) => i + 1), // Matthew 27 has 66 verses
    28: Array.from({ length: 20 }, (_, i) => i + 1), // Matthew 28 has 20 verses
}
