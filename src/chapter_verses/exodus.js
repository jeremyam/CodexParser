module.exports = {
    1: Array.from({ length: 22 }, (_, i) => i + 1), // Exodus 1 has 22 verses
    2: Array.from({ length: 25 }, (_, i) => i + 1), // Exodus 2 has 25 verses
    3: Array.from({ length: 22 }, (_, i) => i + 1), // Exodus 3 has 22 verses
    4: Array.from({ length: 31 }, (_, i) => i + 1), // Exodus 4 has 31 verses
    5: Array.from({ length: 23 }, (_, i) => i + 1), // Exodus 5 has 23 verses
    6: Array.from({ length: 30 }, (_, i) => i + 1), // Exodus 6 has 30 verses
    7: Array.from({ length: 25 }, (_, i) => i + 1), // Exodus 7 has 25 verses
    8: Array.from({ length: 32 }, (_, i) => i + 1), // Exodus 8 has 32 verses
    9: Array.from({ length: 35 }, (_, i) => i + 1), // Exodus 9 has 35 verses
    10: Array.from({ length: 29 }, (_, i) => i + 1), // Exodus 10 has 29 verses
    11: Array.from({ length: 10 }, (_, i) => i + 1), // Exodus 11 has 10 verses
    12: Array.from({ length: 51 }, (_, i) => i + 1), // Exodus 12 has 51 verses
    13: Array.from({ length: 22 }, (_, i) => i + 1), // Exodus 13 has 22 verses
    14: Array.from({ length: 31 }, (_, i) => i + 1), // Exodus 14 has 31 verses
    15: Array.from({ length: 27 }, (_, i) => i + 1), // Exodus 15 has 27 verses
    16: Array.from({ length: 36 }, (_, i) => i + 1), // Exodus 16 has 36 verses
    17: Array.from({ length: 16 }, (_, i) => i + 1), // Exodus 17 has 16 verses
    18: Array.from({ length: 27 }, (_, i) => i + 1), // Exodus 18 has 27 verses
    19: Array.from({ length: 25 }, (_, i) => i + 1), // Exodus 19 has 25 verses
    20: Array.from({ length: 26 }, (_, i) => i + 1), // Exodus 20 has 26 verses
    21: Array.from({ length: 36 }, (_, i) => i + 1), // Exodus 21 has 36 verses
    22: Array.from({ length: 31 }, (_, i) => i + 1), // Exodus 22 has 31 verses
    23: Array.from({ length: 33 }, (_, i) => i + 1), // Exodus 23 has 33 verses
    24: Array.from({ length: 18 }, (_, i) => i + 1), // Exodus 24 has 18 verses
    25: Array.from({ length: 40 }, (_, i) => i + 1), // Exodus 25 has 40 verses
    26: Array.from({ length: 37 }, (_, i) => i + 1), // Exodus 26 has 37 verses
    27: Array.from({ length: 21 }, (_, i) => i + 1), // Exodus 27 has 21 verses
    28: Array.from({ length: 43 }, (_, i) => i + 1), // Exodus 28 has 43 verses
    29: Array.from({ length: 46 }, (_, i) => i + 1), // Exodus 29 has 46 verses
    30: Array.from({ length: 38 }, (_, i) => i + 1), // Exodus 30 has 38 verses
    31: Array.from({ length: 18 }, (_, i) => i + 1), // Exodus 31 has 18 verses
    32: Array.from({ length: 35 }, (_, i) => i + 1), // Exodus 32 has 35 verses
    33: Array.from({ length: 23 }, (_, i) => i + 1), // Exodus 33 has 23 verses
    34: Array.from({ length: 35 }, (_, i) => i + 1), // Exodus 34 has 35 verses
    35: Array.from({ length: 35 }, (_, i) => i + 1), // Exodus 35 has 35 verses
    36: Array.from({ length: 38 }, (_, i) => i + 1), // Exodus 36 has 38 verses
    37: Array.from({ length: 29 }, (_, i) => i + 1), // Exodus 37 has 29 verses
    38: Array.from({ length: 31 }, (_, i) => i + 1), // Exodus 38 has 31 verses
    39: Array.from({ length: 43 }, (_, i) => i + 1), // Exodus 39 has 43 verses
    40: Array.from({ length: 38 }, (_, i) => i + 1), // Exodus 40 has 38 verses
}
